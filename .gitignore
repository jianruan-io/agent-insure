.DS_Store
node_modules/
dist/

# Playwright output
test-results/
playwright-report/

# Secrets — .env.example stays tracked, everything else doesn't
.env
.env.local
.env.*.local

# Ideation brainstorm scratchpad
tools/journey-tracker/

# Documentation / other references dump (top-level only — NOT .claude/skills/*/references/,
# which are real, tracked skill content; a bare pattern here would silently ignore those too)
/references/
